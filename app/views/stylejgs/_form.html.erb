<%= form_for(stylejg) do |f| %>
  <% if stylejg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stylejg.errors.count, "error") %> prohibited this stylejg from being saved:</h2>

      <ul>
      <% stylejg.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :context %>
    <%= f.text_area :context %>
  </div>
  
  <!--<div class="field">-->
  <!--  <%= f.label :image %>-->
  <!--  <%= f.file_field :image, accept: 'image/png,image/gif,image/jpeg' %>-->
  <!--</div>-->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
