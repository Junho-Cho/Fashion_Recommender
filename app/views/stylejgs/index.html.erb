<p id="notice"><%= notice %></p>

<h1>Stylejgs</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Context</th>
      <th>Image</th>
      <th>Rate</th>
      <th colspan="5"></th>
    </tr>
  </thead>

  <tbody>
    <% @stylejgs.each do |stylejg| %>
      <tr>
        <td><%= stylejg.title %></td>
        <td><%= stylejg.context %></td>
        <td><%= image_tag("#{stylejg.image.thumb}") %></td>
        <td><%= stylejg.rates.size %>명</td>
        <td><%= link_to 'Show', stylejg %></td>
        <td><%= link_to 'Edit', edit_stylejg_path(stylejg) %></td>
        <td><%= link_to 'Destroy', stylejg, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Stylejg', new_stylejg_path %>
